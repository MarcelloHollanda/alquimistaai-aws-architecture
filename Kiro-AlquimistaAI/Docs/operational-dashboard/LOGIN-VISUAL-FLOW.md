# 🎨 Fluxo Visual - Sistema de Login

## 🗺️ Mapa de Rotas

```
┌─────────────────────────────────────────────────────────────┐
│                    SISTEMA DE LOGIN                          │
│                   AlquimistaAI Dashboard                     │
└─────────────────────────────────────────────────────────────┘

┌──────────────────┐
│   Usuário        │
│   Acessa         │
└────────┬─────────┘
         │
         ▼
    ┌────────┐
    │  URL?  │
    └───┬────┘
        │
        ├─────────────────────────────────────────┐
        │                                         │
        ▼                                         ▼
┌───────────────┐                        ┌──────────────┐
│  /auth/login  │                        │    /login    │
│   (OFICIAL)   │                        │ (REDIRECT)   │
└───────┬───────┘                        └──────┬───────┘
        │                                       │
        │                                       │ Redirect
        │                                       │ automático
        │                                       │
        │                                       ▼
        │                                ┌──────────────┐
        │                                │  /auth/login │
        │                                └──────┬───────┘
        │                                       │
        └───────────────┬───────────────────────┘
                        │
                        ▼
            ┌───────────────────────┐
            │  Página de Login      │
            │  - Título             │
            │  - Botão Cognito      │
            │  - Mensagem SSO       │
            └───────────┬───────────┘
                        │
                        │ Clique no botão
                        ▼
            ┌───────────────────────┐
            │  Cognito OAuth        │
            │  - Autenticação       │
            │  - Autorização        │
            └───────────┬───────────┘
                        │
                        │ Sucesso
                        ▼
            ┌───────────────────────┐
            │  /auth/callback       │
            │  - Processa tokens    │
            │  - Cria sessão        │
            └───────────┬───────────┘
                        │
                        ▼
            ┌───────────────────────┐
            │  Dashboard            │
            │  /app/dashboard       │
            └───────────────────────┘
```

---

## 🔄 Fluxo de Redirecionamento

```
┌─────────────────────────────────────────────────────────────┐
│              FLUXO DE PROTEÇÃO DE ROTAS                      │
└─────────────────────────────────────────────────────────────┘

Usuário não autenticado tenta acessar rota protegida:

┌──────────────────┐
│ /app/dashboard   │
│ (Protegida)      │
└────────┬─────────┘
         │
         │ Middleware detecta
         │ falta de autenticação
         ▼
┌──────────────────────────────────────┐
│ Redirect para:                       │
│ /auth/login?redirect=/app/dashboard  │
└────────┬─────────────────────────────┘
         │
         │ Usuário faz login
         ▼
┌──────────────────┐
│ Cognito OAuth    │
└────────┬─────────┘
         │
         │ Sucesso
         ▼
┌──────────────────┐
│ /auth/callback   │
└────────┬─────────┘
         │
         │ Lê parâmetro redirect
         ▼
┌──────────────────┐
│ /app/dashboard   │
│ (Agora permitido)│
└──────────────────┘
```

---

## 📁 Estrutura de Arquivos

```
frontend/src/
│
├── app/
│   │
│   ├── (auth)/                    ← Grupo de rotas públicas
│   │   └── login/
│   │       └── page.tsx           ← Redirect: /login → /auth/login
│   │
│   ├── auth/                      ← Rotas de autenticação
│   │   ├── login/
│   │   │   └── page.tsx           ← ✅ PÁGINA OFICIAL DE LOGIN
│   │   ├── callback/
│   │   │   └── page.tsx           ← Callback OAuth
│   │   ├── logout/
│   │   │   └── page.tsx           ← Logout
│   │   └── logout-callback/
│   │       └── page.tsx           ← Callback de logout
│   │
│   └── (dashboard)/               ← Grupo de rotas protegidas
│       └── dashboard/
│           └── page.tsx           ← Dashboard principal
│
├── lib/
│   ├── constants.ts               ← ROUTES.LOGIN = '/auth/login'
│   └── cognito-client.ts          ← Cliente Cognito
│
└── middleware.ts                  ← Proteção de rotas
```

---

## 🎯 Decisões de Rota

```
┌─────────────────────────────────────────────────────────────┐
│                    POR QUE /auth/login?                      │
└─────────────────────────────────────────────────────────────┘

✅ VANTAGENS:
├─ Agrupa todas as rotas de autenticação sob /auth/*
├─ Consistente com /auth/callback, /auth/logout
├─ Facilita configuração de middleware
├─ Padrão comum em aplicações Next.js
└─ Melhor organização de código

❌ ALTERNATIVA /login:
├─ Mais curta, mas menos organizada
├─ Não agrupa com outras rotas auth
├─ Mantida apenas para compatibilidade
└─ Redireciona automaticamente para /auth/login
```

---

## 🔐 Fluxo de Segurança

```
┌─────────────────────────────────────────────────────────────┐
│                  CAMADAS DE SEGURANÇA                        │
└─────────────────────────────────────────────────────────────┘

1. MIDDLEWARE (frontend/middleware.ts)
   ├─ Intercepta todas as requisições
   ├─ Verifica se rota é pública
   ├─ Se protegida: verifica autenticação
   └─ Redireciona para /auth/login se necessário

2. COGNITO OAUTH
   ├─ Autenticação externa segura
   ├─ Tokens JWT
   ├─ Refresh tokens
   └─ Session management

3. ROUTE GUARDS (Componentes)
   ├─ Verificação adicional no cliente
   ├─ Proteção de componentes sensíveis
   └─ Fallback para loading/error states

4. API PROTECTION (Backend)
   ├─ Validação de tokens em cada request
   ├─ Verificação de permissões
   └─ Rate limiting
```

---

## 📊 Matriz de Rotas

```
┌──────────────────┬──────────┬────────────┬─────────────────┐
│ Rota             │ Tipo     │ Auth?      │ Descrição       │
├──────────────────┼──────────┼────────────┼─────────────────┤
│ /                │ Pública  │ Não        │ Landing page    │
│ /login           │ Pública  │ Não        │ Redirect        │
│ /auth/login      │ Pública  │ Não        │ Login oficial   │
│ /auth/callback   │ Pública  │ Não        │ OAuth callback  │
│ /auth/logout     │ Pública  │ Não        │ Logout          │
│ /app/*           │ Protegida│ Sim        │ Dashboard       │
│ /api/*           │ Protegida│ Sim        │ API routes      │
└──────────────────┴──────────┴────────────┴─────────────────┘
```

---

## 🧪 Fluxo de Testes

```
┌─────────────────────────────────────────────────────────────┐
│                    ESTRATÉGIA DE TESTES                      │
└─────────────────────────────────────────────────────────────┘

1. TESTES UNITÁRIOS
   └─ Componentes individuais
      ├─ LoginPage
      ├─ RedirectPage
      └─ Middleware logic

2. TESTES DE INTEGRAÇÃO
   └─ Fluxos completos
      ├─ Login → Callback → Dashboard
      ├─ Redirect /login → /auth/login
      └─ Protected route → Login → Back

3. TESTES E2E
   └─ Simulação de usuário real
      ├─ Playwright/Cypress
      ├─ Fluxo completo de autenticação
      └─ Casos de erro

4. TESTES DE SEGURANÇA
   └─ Tentativas de bypass
      ├─ Acesso direto a rotas protegidas
      ├─ Tokens inválidos
      └─ Session hijacking
```

---

## 🎨 Estados da UI

```
┌─────────────────────────────────────────────────────────────┐
│                    ESTADOS DO LOGIN                          │
└─────────────────────────────────────────────────────────────┘

1. INICIAL
   ┌──────────────────────────┐
   │ 🔐 Login                 │
   │                          │
   │ [Entrar com Cognito]     │
   │                          │
   │ ℹ️ Login Único: Use suas │
   │    credenciais...        │
   └──────────────────────────┘

2. LOADING (Redirect)
   ┌──────────────────────────┐
   │ ⏳ Redirecionando...     │
   │                          │
   │      [Spinner]           │
   │                          │
   └──────────────────────────┘

3. COGNITO (Externa)
   ┌──────────────────────────┐
   │ 🔐 AWS Cognito           │
   │                          │
   │ Email: [_________]       │
   │ Senha: [_________]       │
   │                          │
   │ [Entrar]                 │
   └──────────────────────────┘

4. CALLBACK (Processing)
   ┌──────────────────────────┐
   │ ✅ Autenticando...       │
   │                          │
   │      [Spinner]           │
   │                          │
   └──────────────────────────┘

5. ERRO
   ┌──────────────────────────┐
   │ ❌ Erro ao autenticar    │
   │                          │
   │ Mensagem de erro aqui    │
   │                          │
   │ [Tentar novamente]       │
   └──────────────────────────┘
```

---

## 🔄 Ciclo de Vida da Sessão

```
┌─────────────────────────────────────────────────────────────┐
│                  GERENCIAMENTO DE SESSÃO                     │
└─────────────────────────────────────────────────────────────┘

Login
  │
  ├─ Cognito retorna tokens
  │  ├─ Access Token (1h)
  │  ├─ ID Token (1h)
  │  └─ Refresh Token (30d)
  │
  ├─ Tokens armazenados
  │  ├─ HttpOnly Cookie (seguro)
  │  └─ Session Storage (temporário)
  │
  ├─ Usuário navega
  │  ├─ Access Token validado em cada request
  │  └─ Middleware verifica expiração
  │
  ├─ Token expira (1h)
  │  ├─ Refresh automático com Refresh Token
  │  └─ Novo Access Token obtido
  │
  └─ Refresh Token expira (30d)
     └─ Usuário precisa fazer login novamente
```

---

## 📱 Responsividade

```
┌─────────────────────────────────────────────────────────────┐
│                    DESIGN RESPONSIVO                         │
└─────────────────────────────────────────────────────────────┘

DESKTOP (≥1024px)
┌────────────────────────────────────┐
│  Logo              [User Menu]     │
├────────────────────────────────────┤
│                                    │
│         🔐 Login                   │
│                                    │
│    [Entrar com Cognito]            │
│                                    │
│    ℹ️ Login Único: Use suas        │
│       credenciais corporativas     │
│                                    │
└────────────────────────────────────┘

TABLET (768px - 1023px)
┌──────────────────────────┐
│  Logo         [Menu]     │
├──────────────────────────┤
│                          │
│      🔐 Login            │
│                          │
│  [Entrar com Cognito]    │
│                          │
│  ℹ️ Login Único          │
│                          │
└──────────────────────────┘

MOBILE (<768px)
┌────────────────┐
│  Logo    [≡]   │
├────────────────┤
│                │
│   🔐 Login     │
│                │
│  [Cognito]     │
│                │
│  ℹ️ SSO        │
│                │
└────────────────┘
```

---

## ✅ Checklist Visual

```
┌─────────────────────────────────────────────────────────────┐
│                  VALIDAÇÃO VISUAL                            │
└─────────────────────────────────────────────────────────────┘

LAYOUT
├─ [ ] Logo visível
├─ [ ] Título centralizado
├─ [ ] Botão destacado
├─ [ ] Mensagem informativa clara
└─ [ ] Footer com links

CORES
├─ [ ] Contraste adequado (WCAG AA)
├─ [ ] Botão primário em destaque
├─ [ ] Estados hover/focus visíveis
└─ [ ] Mensagens de erro em vermelho

TIPOGRAFIA
├─ [ ] Fonte legível (≥16px)
├─ [ ] Hierarquia clara (h1, h2, p)
├─ [ ] Line-height adequado
└─ [ ] Espaçamento consistente

INTERATIVIDADE
├─ [ ] Botão responde ao hover
├─ [ ] Loading state visível
├─ [ ] Transições suaves
└─ [ ] Feedback visual em ações

ACESSIBILIDADE
├─ [ ] Navegação por teclado
├─ [ ] Screen reader friendly
├─ [ ] Labels descritivos
└─ [ ] Contraste de cores adequado
```

---

**Última Atualização:** 2024-11-19  
**Mantido por:** Equipe AlquimistaAI  
**Versão:** 1.0.0
